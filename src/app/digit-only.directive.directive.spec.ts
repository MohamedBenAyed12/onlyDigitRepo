import { DigitOnly.DirectiveDirective } from './digit-only.directive.directive';

describe('DigitOnly.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DigitOnly.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
